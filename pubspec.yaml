name: po_tracker
description: A pure Dart backend logic engine for progressive overload.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  test: ^1.25.6

flutter:
  uses-material-design: true
  assets:
    - assets/data/exercises.json
